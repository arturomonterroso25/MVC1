'user strict'

const { create } = require('lodash');
const Sequelize = require('sequelize');
const Proveedores = ('../models/Proveedores');

module.exports = {
    get(req, res) {

    },

    create(req, res) {

    },

    update(req, res) {

    },

    delete(req, res) {

    }
}