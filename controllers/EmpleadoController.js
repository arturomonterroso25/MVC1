'user strict'

const { create } = require('lodash');
const Sequelize = require('sequelize');
const Empleado = ('../models/Empleado');

module.exports = {
    get(req, res) {

    },

    create(req, res) {

    },

    update(req, res) {

    },

    delete(req, res) {

    }
}